# Arc Testnet  (chain id 5042002). Gas is paid in USDC.
ARC_RPC_URL=https://rpc.testnet.arc.network
ARC_EXPLORER_URL=https://testnet.arcscan.app

# Deploy inputs — DO NOT commit real values here. Put them in .env instead.
PRIVATE_KEY=0x
# Arc USDC (6 decimals, verified on-chain). Used as both stablecoin AND gas.
PATHUSD_ADDRESS=0x3600000000000000000000000000000000000000
TREASURY_ADDRESS=0x
